{
  "name": "trustmap",
  "version": "2.14.0",
  "private": true,
  "type": "module",
  "description": "trustmap \u2014 security-focused fork of Archify: JSON-IR diagram renderers (architecture / workflow / sequence / dataflow / lifecycle) for smart-contract threat-model maps.",
  "license": "MIT",
  "bin": {
    "trustmap": "./bin/archify.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "generate:validators": "node scripts/generate-validators.mjs",
    "check:validators": "node scripts/generate-validators.mjs --check",
    "check:release-identity": "node ../scripts/check-release-identity.mjs",
    "build:gallery": "node ../scripts/build-gallery.mjs ../docs",
    "build:guide": "node ../scripts/build-guide.mjs ../docs/guide.html",
    "build:start": "node ../scripts/build-start.mjs ../docs/start.html",
    "test:webm": "node test/webm-artifact.smoke.mjs",
    "test": "npm run check:validators && npm run check:release-identity && node test/golden.mjs && node ../scripts/run-tests.mjs",
    "render:examples": "node scripts/render-examples.mjs ../examples"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
